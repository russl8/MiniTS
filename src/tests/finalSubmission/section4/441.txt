class f441pass {
  x: int = 1;
  y: int = 2;
  
  // OK: arithmetic operands are int.  
  z: int = x + y * (x - 1); 
}

class f441fail {
  x: int = 1;
  y: bool = True;
  
  // Fail: y is bool, used in arithmetic.
  z: int = x + y;
}