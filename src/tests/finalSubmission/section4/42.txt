class p42 {
  // OK
  function f(): int {
      return 3;
  }
}

class f42 {

  // FAIL: returning int 
  // but declared bool return value
  function g(): bool {
      return 1;
  }
  
  // FAIL: returning list[char] 
  // but declared list[int] return value
  function h(): list[int] {
      return "ab";
  }
}