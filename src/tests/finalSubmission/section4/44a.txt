class p44a {
  x: int = 1;
  y: int = 2;
  
  // OK
  z: int = x + y * (x - 1);
}


class f44a {
  x: int = 1;
  y: bool = True;
  
  // FAIL: arithmetic expression with bool value
  z: int = x + (1 && y);
}
