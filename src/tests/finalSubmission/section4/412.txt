class f412pass {
  a: list[int] = [1, 2];
  
  // Pass: list items are ints.
  a = [3, 4, 5];
}

class f412fail {
  a: list[char] = "this is valid";
  
  // Fail: True is not char.
  a = ['h', True];
}