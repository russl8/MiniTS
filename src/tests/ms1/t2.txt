class MyClass {
    x : int = 5;
    y : bool;
    y = False;
    
    // this if statement should be true
    if (x > 0 && !y ) { 
        x = x + 1; // x=6
		x = x + 1; // x=7
    }

        // this if statement should be true
    if (x > 0 && !x ) { 
        x = x + 1; // x=6
		x = x + 1; // x=7
    }
}