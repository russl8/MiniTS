<<<<<<<< HEAD:src/tests/M1/t3.txt
class MyClass extends dsadasdasdsaBaseClass {
    x : inadsdast = 5;dasdasd
========
class MyClass {
    x : int = 5;
>>>>>>>> master:src/tests/ms1/t3.txt
    y : bool;
    z : int = 10;
    b : bool = True;

    y = False;

    if (x > 0 && y) {
        //  Error: Arithmetic expression used in logical OR
        x = (5 + 3) || False;
        
        //  Error: INT used in logical AND
        x = 5 && y;
        
        //  Error: Logical OR between INT values
        x = 2 || 5;
        
        //  Error: BOOL used in arithmetic ADD
        x = y + 4;
        
        //  Error: BOOL in relational comparison with INT
        y = y > 5;
        
        //  Error: Type mismatch in equality expression (INT vs BOOL)
        y = x == y;
        
        //  Error: NOT applied to INT
        y = !5;
        
        //  Error: Logical OR between INTs
        x = x || x;
        
        //  Error: Equality of different type
        y = x == (True && False);
        
        // Error : spot type mismatch in many parentheses
        y = (((x))) == (True && False);
        
         //  Error: Equality of different type (nested)
        y = x ==  (True && ( 2 <= (False) ) ) ;
        
        //  Valid: Relational
        y = x < z;
        
        //  Valid: Equality of same type
        y = y == (True && False);
        
        //  Valid: Arithmetic
        x = x + z;
        
        //  Valid: Logical AND between bools
        y = y && b;
        
        //  Valid: NOT on bool
        y = !y;
    }
}
