class MyClass {
    x : INT = 5;
    y : BOOL;
    y = False;
	z : BOOL;
    
    IF (!y) {
        y = y; 
      	y = False || (((( !y ) && True)));  // y should now be true
      	x = x * 2;							// x should now be 10
    }
}